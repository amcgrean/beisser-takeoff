# Agent Handoff — 2026-04-04 (beisser-takeoff / LiveEdge)

**Branch**: `main`

## What was done this session

### 1. Page visit tracking — fully live
- **Applied `bids.page_visits` migration** in Supabase (via MCP — no manual SQL needed)
- **Created `src/hooks/usePageTracking.ts`** — shared hook, uses `usePathname`, fires `POST /api/track-visit` on mount
- **Added `usePageTracking()` to all 30 module main client pages** — every list/hub page now tracks visits. Skipped add/manage/detail sub-pages and `HomeClient.tsx` (has its own inline tracking)
- Quick Access strip on homepage will now populate as users navigate

### 2. RMA Credits — database ready, ingest pipeline planned
- Added `r2_key TEXT` column to `public.credit_images` via Supabase MCP
- Confirmed R2 bucket structure: `credits/{rma_number}/{filename}` (isolated from `takeoff/` and `bids/` prefixes)
- Designed full Resend inbound email pipeline (not yet built — on todo list):
  - `*@rma.beisser.cloud` MX → Resend inbound → `POST /api/inbound/credits`
  - Svix webhook verification, RMA# parsed from subject, image attachments uploaded to R2
  - Upserts `public.credit_images` with `r2_key`, `email_from`, `email_subject`, `received_at`
  - Handles both attached and inline/embedded images
  - Needs: DNS MX record, Resend inbound route, `RESEND_WEBHOOK_SECRET` env var, `svix` dep

---

## Pending actions (priority order)

### 1. RMA Credits ingest email (next up)
**Infrastructure (manual):**
- Add MX record: `rma.beisser.cloud` → Resend inbound MX
- Create inbound route in Resend dashboard: `*@rma.beisser.cloud` → `https://liveedge.vercel.app/api/inbound/credits`
- Add `RESEND_WEBHOOK_SECRET` to Vercel env vars

**Code to build:**
```
app/api/inbound/credits/route.ts   — webhook handler
app/api/credits/[id]/image/route.ts — presigned URL for serving
```
Update `CreditsClient.tsx` to show image thumbnails once `r2_key` is populated.
One new dep: `svix` (webhook signature verification).

### 2. ERP API tables → views migration
User noted ERP tables are moving to views — this will eliminate join logic currently in API routes.
As views roll out, update affected routes (sales, dispatch, warehouse, purchasing) to query views directly.

### 3. Flask sunset
DNS cutover + archive `C:\Users\amcgrean\python\wh-tracker-fly\WH-Tracker` after user testing confirms parity.

### 4. Purchasing workflow gaps (blocked)
Before building, verify these WH-Tracker tables exist in `public` schema:
```sql
SELECT table_name FROM information_schema.tables
WHERE table_schema = 'public'
AND table_name IN ('purchasing_tasks','purchasing_approvals','purchasing_notes','purchasing_exceptions');
```

### 5. Suggested Buys (blocked)
`app_purchasing_queue` view confirmed missing. Check:
```sql
SELECT table_name FROM information_schema.tables
WHERE table_schema = 'public' AND table_name ILIKE '%suggested%';
```

---

## Key files changed this session
| File | Change |
|------|--------|
| `src/hooks/usePageTracking.ts` | New — shared page tracking hook |
| `db/migrations/0004_page_visits.sql` | Applied in Supabase ✅ |
| 30× `*Client.tsx` | Added `usePageTracking()` import + call |

## Known state
- `bids.page_visits` table is live — Quick Access on homepage will start showing data after users navigate
- `public.credit_images.r2_key` column is live — ready for upload script / ingest webhook
- `HomeClient.tsx` tracks `/` inline (not via hook) — intentional, leave as-is
